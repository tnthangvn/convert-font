'use strict';

// Canonical implementation now lives in lib/naming.js (shared by server, MCP,
// and CLI). Re-exported here for back-compat with the rv-icon skill + tests.
module.exports = require('../../../lib/naming');
